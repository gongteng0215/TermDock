# 安全说明

[English](SECURITY.md)

TermDock 是 SSH/SFTP 桌面客户端，因此凭据处理和本地数据边界非常重要。

## 本地优先设计

- TermDock 不需要云账号来管理服务器。
- 会话数据、传输历史、设置、日志和诊断信息都存储在运行应用的本机。
- 诊断信息和 bug report 包在本地生成，你可以先检查内容再决定是否分享。

## 凭据

- TermDock 通过应用凭据层保存连接密钥，在可用时使用操作系统安全存储。
- 会话和分组 JSON 导出会刻意排除解密后的凭据。
- 导入会话数据后应先检查再保存，尤其是来自其他机器或团队成员的数据。

## 诊断

Bug report 导出可能包含运行时元数据、日志、设置快照和断连报告。上传到公开 issue 或发送给其他人前，请先检查生成的压缩包。

## 当前限制

- 数据持久化仍基于 JSON，SQLite 迁移仍在计划中。
- 公开可信签名 / notarization 证据仍在完善。
- 暂无应用内自动更新。
- 尚未实现团队共享 vault 同步。

## 报告安全问题

请不要在问题完成分级前公开披露敏感安全问题。

如可用，请通过 GitHub security advisories 提交私密报告；也可以联系维护者，并提供足够复现问题的信息，但不要包含真实生产凭据。
